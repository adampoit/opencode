name: ci

on:
  push:
    branches: [dev, sync/integration]
  pull_request:
    branches: [dev]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Run typecheck
        run: bun typecheck

  unit:
    name: unit (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
            os: ubuntu-latest
          - name: darwin
            os: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Configure git identity
        run: |
          git config --global user.email "bot@opencode.ai"
          git config --global user.name "opencode"

      - name: Run unit tests
        run: bun turbo test

  nix_build:
    name: nix-build (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
            os: ubuntu-latest
          - name: darwin
            os: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Nix
        uses: nixbuild/nix-quick-install-action@v34

      - name: Configure repository cache
        env:
          CACHE_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}
          CACHE_KEY: ${{ vars.NIX_CACHE_PUBLIC_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.config/nix
          printf '%s\n' "extra-substituters = $CACHE_URL" >> ~/.config/nix/nix.conf
          if [ -n "$CACHE_KEY" ]; then
            printf '%s\n' "extra-trusted-public-keys = $CACHE_KEY" >> ~/.config/nix/nix.conf
          fi

      - name: Evaluate flake outputs
        run: |
          set -euo pipefail
          sys=$(nix eval --impure --expr builtins.currentSystem --raw)
          nix --version
          nix flake metadata
          nix flake show --all-systems
          nix eval ".#packages.${sys}.opencode.drvPath" --raw
          nix eval ".#devShells.${sys}.default.drvPath" --raw

      - name: Build Nix package
        run: |
          set -euo pipefail
          sys=$(nix eval --impure --expr builtins.currentSystem --raw)
          nix build ".#packages.${sys}.opencode" --print-out-paths | tee /tmp/static-nix-cache-paths.txt

      - name: Save Nix binary
        if: github.repository == 'adampoit/opencode' && github.event_name == 'push' && github.ref == 'refs/heads/dev'
        uses: adampoit/static-nix-cache/save@master
        with:
          paths-file: /tmp/static-nix-cache-paths.txt
          name: ${{ matrix.name }}
          signing-key: ${{ secrets.NIX_CACHE_SIGNING_KEY }}

  publish_nix_cache:
    name: publish nix cache (${{ matrix.name }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    if: github.repository == 'adampoit/opencode' && github.event_name == 'push' && github.ref == 'refs/heads/dev'
    needs: [typecheck, unit, nix_build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux
          - name: darwin
    permissions:
      contents: write
    steps:
      - name: Setup Nix
        uses: nixbuild/nix-quick-install-action@v34

      - name: Deploy to per-platform release
        uses: adampoit/static-nix-cache/deploy@master
        with:
          artifact-pattern: static-nix-cache-${{ matrix.name }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          github-release-tag: nix-cache-${{ matrix.name }}
          signing-key: ${{ secrets.NIX_CACHE_SIGNING_KEY }}

  generate_nix_cache:
    name: generate nix cache site
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.repository == 'adampoit/opencode' && github.event_name == 'push' && github.ref == 'refs/heads/dev'
    needs: publish_nix_cache
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.pages.outputs.page_url }}
    steps:
      - name: Generate cache site
        uses: adampoit/static-nix-cache/generate-static@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          github-release-tags: nix-cache-linux,nix-cache-darwin
          static: ./site

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

      - name: Deploy GitHub Pages site
        id: pages
        uses: actions/deploy-pages@v4
